# Shared packages

Optional workspace packages shared across the FORM storefront variations.

Keep sharing deliberately minimal — the variations are intentionally different
in code, mechanics, and UI/UX. Good candidates for this folder:

- design tokens / brand config
- shared `tsconfig` and ESLint config
- small typed utilities with no UI opinions
