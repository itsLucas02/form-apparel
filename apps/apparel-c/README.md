# @form/apparel-c

Third FORM storefront variation — **planned**.

A distinct storefront for the same FORM brand, with its own code, mechanics,
and UI/UX direction. The app scaffold will live here once it is ready.

Run from the repo root with the workspace tooling (`pnpm --filter @form/apparel-c dev`)
once this app is scaffolded.
